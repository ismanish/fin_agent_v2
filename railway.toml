[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && python init_deployment.py"

[deploy]
startCommand = "uvicorn app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 3